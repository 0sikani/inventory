# Production Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/inventory1?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=your_password

# JPA Configuration (if using JPA later)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.format_sql=false

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Logging
logging.level.com.studentmanagementsystem=INFO
logging.file.name=logs/student-management-api.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n